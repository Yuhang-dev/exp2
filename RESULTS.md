# RTX 4090 实验结果：4K–128K

用户远端已完成 kernel 检查、原始 4K–32K 生成基线、YaRN 4× 的 4K–128K 位置实验及原始 RoPE 32K 对照。数据来自用户提供的 [汇总报告](results/study_4090/REPORT.md)，下述解读依据报告中的聚合值；逐 token 原始文件和各次检查误差保存在远端 `results/study/`。

统一条件：Qwen2.5-7B-Instruct、BF16、batch=1、RTX 4090 24GB；4 个 gov_report 文档拼接流，每种方法每个流计时 3 次。速度是 MLP/RMSNorm 分块、`use_cache=False` 下的完整模型前向加末位置 LM head/argmax。它不代表保留全部层 KV cache 的生成 TTFT 或 decode 性能。

默认 FlashPrefill 保护最后 2 个 query blocks，主曲线使用同一 YaRN 4× 设置：

| 上下文 | Dense 前向 | FlashPrefill 前向 | 加速比 | 峰值显存 | 保留因果块 | 平均 ΔNLL | ΔNLL P99 | top1 一致率 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 32K | 4.586 s | 3.669 s | 1.25× | 15.80 GiB | 21.0% | +0.0045 | 1.0289 | 92.0% |
| 64K | 11.877 s | 7.452 s | 1.59× | 17.41 GiB | 12.3% | +0.0226 | 1.4075 | 89.7% |
| 128K | 34.724 s | 15.255 s | 2.28× | 20.63 GiB | 7.0% | +0.0009 | 1.2344 | 88.8% |

这里的误差指标来自整篇前向的采样 token。块选择依赖同块后续 query，因此这些指标衡量输出差异，不能直接作为严格自回归 PPL；top1 一致率也不是任务准确率。

**平均误差接近零，仍存在少数位置的明显概率损失。** 128K 默认设置的平均 ΔNLL 只有 +0.0009，但 P99 为 1.2344、ΔNLL>1 的比例为 1.7%、平均 KL 为 0.08389。根据 ΔNLL=log(p_dense/p_sparse)，差值达到 1.2344 时真实 token 的概率约为 Dense 的 29%。这说明当前采样平均值没有完整呈现分布变化；尚不能据此断言某项长文任务能力下降。

64K 的 late 窗口更值得定位：平均 ΔNLL 为 +0.0912、P99 为 2.9379，而全体平均 ΔNLL 为 +0.0226。该窗口在约 90% 位置，位于末尾保护区之外。不同长度的采样位置和文本内容会变化，不能把这一差异单独归因于长度。

**末尾保护改变了末尾测量，但不能消除全文的表示差异。** 128K 从 tail0 改到 tail2，完整前向由 14.945 s 增加到 15.255 s，约增加 2.1%；最后一个 block 的 ΔNLL P99 从 1.5781 降到 1.0411，降幅约 34%。early/middle/late 区域的误差保持相同；全文平均 KL 从 0.09598 降到 0.08389。因此只抽取末尾位置会改变对这套方法的评价。保护更多块也不保证真实 token NLL 单调改善：128K 整篇平均 ΔNLL 由 tail0 的 -0.0077 变为 tail2 的 +0.0009。

**同长度下，位置编码设置与稀疏误差存在交互。** 32K、同一批输入、tail2：原始 RoPE 保留 17.0% 因果块，相对其 Dense 的 KL 为 0.01749；YaRN 4× 保留 21.0%，相对其 Dense 的 KL 为 0.03434，约为前者的 1.96 倍。YaRN 下保留更多块，分布偏差仍更大。这是可进一步研究的敏感性现象：需要区分 proxy 排序、实际注意力分布变化及跨层误差传播，当前结果尚未定位是哪一种。

只输入前缀的检查每个设置只有 12 个 probe。128K 行的 tail2 平均 ΔNLL 为 -0.1607、top1 一致率为 75%（9/12），显示概率改善和 argmax 改变可以同时出现。该行实际输入在约 32K/64K/96K 处截断，尚不是 128K 完整历史之后的严格预测检查。

目前可支持的结论是：**这套设置在单张 4090 上完成了 128K 前向，获得约 2.28× 加速；接近零的平均 ΔNLL 同时伴随非零的分布偏差和部分位置明显的概率下降。** 四个文档流的这些诊断结果，还不能证明一般意义上的“长文稀疏注意力没有免费午餐”。

下一步优先分析远端 `worst_tokens.csv`、`per_sample_errors.csv` 和各目录 `prefix.csv`：确认大误差是否集中在某个文档流、数字/专名或跨报告信息；选少量固定目标，只输入其前缀复现，再分析逐层所选块与 Dense 注意力质量。后续验证应固定相同目标位置，并加入接近完整 128K 的前缀；在定位之前不把原因预设为 mean pooling 丢失方向信息。
